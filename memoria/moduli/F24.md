# F24 — stato reale vs specifica

Fonte specifica: `F24 — Flussi automatici — Logica relazionale completa.txt` (fornita
dall'utente). Verificato leggendo il codice attuale (post-consolidamento router del
2026-07-07).

## Canale di importazione (confermato, non serve correzione "Aruba")

L'F24 non entra mai via Aruba/PEC/Drive fatture: entra via email, instradato dalla tabella
`mittenti_email` — Rosaria Marotta (commercialista) → F24 fiscali, Studio Ferrantini
(consulente lavoro) → F24 contributivi (`app/routers/f24/email_f24.py:38-39`, config sender
in `app/routers/f24_email_settings.py:52,60,68`). Questo è coerente con la tabella reale dei
mittenti attendibili fornita dall'utente (righe 2-3: Ferrantini/Marotta → cedolini/F24 →
contabilità).

## Cosa è confermato implementato

| Requisito spec | Stato | Evidenza |
|---|---|---|
| Import F24 da email (quietanza + F24 originale) | ✅ | `email_f24.py::processa_allegati_f24`, prova prima `parse_f24_commercialista` poi fallback `parse_quietanza_f24` |
| Matching F24↔banca per importo+data (tolleranza) | ✅ ma solo binario | `estratto_conto_bpm_parser.py::riconcilia_f24_con_estratto`, tolleranza ±0.01€ e ±3 giorni |
| Alert scadenza F24 in avvicinamento/scaduto | ✅ | `app/services/handlers/f24_handlers.py`, alert `F24_ATTESO_NON_ACQUISITO`/`F24_NON_PAGATO`/`F24_SCADUTO` triggerati da `app/scheduler.py:106` |

## Gap confermati (in ordine di priorità)

1. **Nessuno dei 9 stati della spec esiste — anzi esistono DUE vocabolari di stato
   incoerenti tra loro, su due collezioni diverse**:
   - `StatoF24` enum (`app/services/f24_gestione_avanzata.py:16-23`): `RICEVUTO`,
     `DA_PAGARE`, `PAGATO`, `SOSTITUITO`, `SCADUTO`, `ANNULLATO` — 6 stati, usato su
     `f24_unificato`.
   - Campo `stato_pagamento` (stringhe grezze `"PAGATO"`/`"DA_PAGARE"`) su
     `f24_commercialista`, usato da `riconciliazione_f24_banca.py` ed
     `estratto_conto_bpm_parser.py` — **collezione e vocabolario diversi dal primo**, nessun
     sincronismo garantito tra i due.
   - Nessuno dei due copre gli stati richiesti dalla spec (`previsto`, `acquisito`,
     `da_verificare`, `pronto_pagamento`, `pagato_non_riconciliato`,
     `pagato_riconciliato`, `scaduto_non_pagato`, `duplicato`, `annullato`).
2. **Distinzione "previsto" vs "acquisito" completamente assente**: nessun generatore di
   scadenze F24 attese prima dell'arrivo del documento — un F24 esiste nel sistema SOLO
   dopo essere stato importato da email, mai come previsione/scadenza attesa in anticipo.
3. **Matching F24↔banca binario, nessun caso ambiguo/differenza importo distinto**: la
   funzione prende il **primo match trovato entro tolleranza** (`break` alla prima
   corrispondenza, nessuno scoring/ranking tra candidati multipli) — se più F24 rientrano
   nella tolleranza, la scelta è silenziosa e non segnalata come ambigua. Solo due esiti
   possibili: pagato o non pagato, non i 3 casi richiesti dalla spec (standard/ambiguo/
   differenza importo).
4. **Triplicazione di parser F24**: `app/services/f24_parser.py` (quietanza bancaria),
   `app/services/parser_f24.py` (F24 originale commercialista), e potenzialmente un terzo
   percorso (`app/parsers/f24_parser.py` + `app/routers/f24_parser.py`, presenza confermata
   ma non ancora ispezionati in dettaglio) — rischio di ulteriore duplicazione logica da
   verificare in un audit dedicato.
5. **4 alert su 7 mai generati**: `alert_engine.py` definisce 7 costanti `F24_*`/`BNK_F24_*`
   (numero corretto secondo spec) ma solo 3 sono effettivamente triggerate
   (`F24_ATTESO_NON_ACQUISITO`, `F24_NON_PAGATO`, `F24_SCADUTO`); `F24_NON_RICONCILIATO`,
   `F24_DUPLICATO`, `F24_PARSER_INCOMPLETO`, `BNK_F24_NON_RICONCILIATO` sono definite ma
   morte.

## Bug/incoerenze note (da correggere)

- **Priorità architetturale**: unificare `f24_unificato` e `f24_commercialista` (o
  documentare esplicitamente perché sono due entità separate) — oggi un F24 può risultare
  "pagato" in una collezione e non nell'altra senza alcun meccanismo di sincronizzazione
  verificato.
- Verificare se `app/parsers/f24_parser.py`/`app/routers/f24_parser.py` sono vivi o dead
  code residuo — non ancora determinato in questo passaggio.
