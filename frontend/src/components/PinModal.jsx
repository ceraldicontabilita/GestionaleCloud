import React from 'react';
import { LockKeyhole, Delete, X } from 'lucide-react';
import { createPinModal } from '../../../frontend_shared/PinModal';

export default createPinModal(React, { LockKeyhole, Delete, X });
