Campo di testo del Ceraldi ERP. Focus navy con anello soffuso; `error` per la validazione; `iconLeft` per la ricerca.

```jsx
<Input placeholder="Cerca fornitore…" iconLeft={<Search size={15} />} />
<Input type="number" error />
```
